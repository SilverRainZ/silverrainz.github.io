name: sphinx-pages
on: [push]
jobs:
  build:
    name: Publish Sphinx Pages
    runs-on: ubuntu-latest
    steps:
    - uses: sphinx-notes/sphinx-pages@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        create_readme: true
